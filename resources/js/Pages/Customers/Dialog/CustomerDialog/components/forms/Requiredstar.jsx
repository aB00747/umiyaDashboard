/**
 * Renders an asterisk to indicate a required field in the customer dialog form.
 *
 * @returns {JSX.Element} The rendered asterisk.
 */
export default function Requiredstar() {
    return (
        <span className="customer-dialog-field-required text-red-700">*</span>
    )
}
